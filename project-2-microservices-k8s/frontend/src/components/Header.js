import React from 'react';
import './Header.css';

function Header() {
  return (
    <div className="header">
      <h1>🚀 Microservices Task Manager</h1>
      <p>React + Flask + PostgreSQL + Redis + Kubernetes</p>
    </div>
  );
}

export default Header;
